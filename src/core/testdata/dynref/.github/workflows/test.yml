on:
  push:
    branches:
      - master

name: CI
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@${{ env.CHECKOUT_VERSION }}
      - name: Setup
        uses: actions/setup-go@${{ inputs.go_version }}
